import { createContext } from "react"; //react libery to import create context

export const countContext=createContext(0) //default 0 value set and create context store variable and export also after we used APP.jsx under 